{
  "tasks": [
    {
      "id": "t-001",
      "title": "Typed read/write layer over the repo as a datastore",
      "contract": {
        "inputs": "A fine-grained GitHub PAT and the set of project repos it serves (owner/name), supplied through the config module. Callers name a repo and one of the three state documents.",
        "outputs": "packages/state exports typed read and write functions for state/tasks.json, state/claims.json and state/users.json. Reads return a parsed, validated document. Writes accept a mutation function and commit the result.",
        "invariants": [
          "Write path is: GET blob and sha, mutate in memory, PUT with that sha. On a 409, re-read and re-apply the mutation exactly once, then throw.",
          "Every write is validated against the zod schema before any network call. A document that fails validation never reaches GitHub.",
          "Reads are cached for 30 seconds.",
          "The zod schemas agree with state/schema/*.json — same required fields, same patterns, same enum values.",
          "Node 22, ESM, TypeScript strict. No use of `any`.",
          "The client is repo-parameterised. Three project repos are served by one process — darwin-model, wildplaces and nostia-landing-redesign — so a repo is an argument, never a module-level constant, and the 30s read cache is keyed by repo AND document."
        ],
        "must_not_break": [
          "state/schema/*.json stays the canonical shape; the zod schemas mirror it and never diverge from it unilaterally.",
          "tools/validate-state.js must still pass on any state this library writes.",
          "The repo root stays CommonJS — no \"type\": \"module\" in the root package.json. tools/validate-state.js is CommonJS and the daily routine invokes it with a bare `node tools/validate-state.js`; adding a root type field breaks the routine's only safety gate. packages/state declares its own package.json with \"type\": \"module\"."
        ]
      },
      "open_decisions": [
        "Whether the write API takes a mutation callback (read-modify-write in one call) or exposes read and write separately and leaves the sha juggling to the caller.",
        "Whether the 30s read cache is per-process in-memory or written to disk, and whether a write invalidates the cache for other documents or only its own.",
        "How a 409 that persists after the single retry is surfaced — a typed error the bot can catch and report, or a plain throw."
      ],
      "touched_paths": [
        "packages/state/**"
      ],
      "depends_on": [],
      "issue": 1,
      "state": "merged"
    },
    {
      "id": "t-002",
      "title": "Discord /task command with claim collision detection",
      "contract": {
        "inputs": "A Discord slash command /task project:<name> from a user in state/users.json, handled by discord.js v14 on top of packages/state. The project option is required and offers the configured repos as choices.",
        "outputs": "Within the named project, picks the first task where state is open, every id in depends_on is merged, and touched_paths does not glob-intersect any active claim IN THAT PROJECT. Writes the claim, opens a thread, and posts the contract, the open_decisions list, the issue link and the branch name task/<id>. /board shows every configured project by default, grouped, and accepts an optional project filter.",
        "invariants": [
          "Refuses if the caller already holds a claim, or is not in state/users.json.",
          "Glob intersection agrees with tools/validate-state.js — in particular src/panel/** must be treated as overlapping src/panel/foo.ts.",
          "No LLM call anywhere in the bot.",
          "Two users racing /task cannot both be given overlapping tasks.",
          "A user holds at most one claim across ALL projects, not one per project. Collision detection is per-project, because two repos cannot share a file path."
        ],
        "must_not_break": [
          "state/claims.json stays schema-valid after every command.",
          "A failed claim leaves no partial state — no orphan thread, no half-written claim."
        ]
      },
      "open_decisions": [
        "Whether the race between two simultaneous /task calls is prevented by an in-process queue, by relying on the 409 retry in packages/state, or by an advisory lock document in the repo.",
        "Whether 'first task' means lowest id, oldest issue, or fewest dependents, when several are eligible.",
        "Whether the glob matcher is copied from tools/validate-state.js, imported from a new shared package, or reimplemented against the same test table."
      ],
      "touched_paths": [
        "packages/bot/**"
      ],
      "depends_on": [
        "t-001"
      ],
      "issue": 2,
      "state": "merged"
    },
    {
      "id": "t-003",
      "title": "Integration harness plus one worked end-to-end example",
      "contract": {
        "inputs": "The composed system as it stands on main, exercised through real user paths rather than unit seams.",
        "outputs": "tests/integration/ with a runner and exactly one fully worked example test that a human can read as a template for the rest.",
        "invariants": [
          "Tests exercise composed behaviour, not single functions in isolation.",
          "The suite runs against main with one command and reports pass or fail without a human reading the output.",
          "No test depends on live Discord or a real PAT; anything external is faked at the boundary."
        ],
        "must_not_break": [
          ".github/workflows/pr.yml keeps running only tests/smoke on pull requests — integration runs nightly, not per PR.",
          "The diff guard that stops contributors editing tests/ stays in force."
        ]
      },
      "open_decisions": [
        "Which runner — node:test, vitest, or a hand-rolled script — weighing zero-dependency operation against ergonomics.",
        "Where the boundary fake lives: an injected transport, a local HTTP stub, or recorded fixtures replayed from disk.",
        "Whether the nightly workflow opens one rolling issue labelled integration-break and reuses it, or a fresh issue per failing night."
      ],
      "touched_paths": [
        "tests/integration/**",
        ".github/workflows/nightly.yml"
      ],
      "depends_on": [],
      "issue": 3,
      "state": "claimed"
    },
    {
      "id": "t-004",
      "title": "Bicep and OIDC deploy workflow for the bot on Azure Container Apps",
      "contract": {
        "inputs": "The bot's container image built from packages/bot's Dockerfile (from t-002), and Azure subscription/resource-group values supplied as GitHub Actions repository variables and OIDC federated credentials — no stored secret.",
        "outputs": "deploy/ containing a Bicep template that provisions an Azure Container App (min replicas 1, no ingress, outbound websocket only) reading the Discord token and GitHub PAT from Key Vault, plus .github/workflows/deploy.yml that builds and pushes the image and runs `az deployment group create` against that template on every push to main that changes packages/bot/** or deploy/**.",
        "invariants": [
          "Authentication to Azure is via OIDC federated credentials (azure/login with no client secret); no service principal secret is stored in GitHub or in the repo.",
          "The Container App is provisioned with minReplicas 1 and no external ingress, matching the outbound-only websocket connection the bot needs.",
          "Discord token and GitHub PAT are read from Key Vault at deploy time; neither value appears in the workflow YAML or in deploy/ in plaintext."
        ],
        "must_not_break": [
          "packages/bot/** stays the only place the bot's application code and Dockerfile live; deploy/ contains only infrastructure definitions.",
          ".github/workflows/pr.yml keeps running on pull_request only; deploy.yml must trigger on push to main, never on pull_request, since it needs write credentials."
        ]
      },
      "open_decisions": [
        "Whether the workflow rebuilds and pushes the image on every push to main regardless of which files changed, or only when packages/bot/** or deploy/ changes — simplicity versus wasted deploys.",
        "Whether Key Vault references are wired into the Container App as environment variables synced by Bicep, or mounted as a secret volume via the Key Vault CSI-equivalent — different blast radius if a value rotates.",
        "Whether the container registry is Azure Container Registry provisioned by this same Bicep template, or an existing registry referenced by name — owning more infrastructure versus depending on a manual prerequisite."
      ],
      "touched_paths": [
        "deploy/**",
        ".github/workflows/deploy.yml"
      ],
      "depends_on": [
        "t-002"
      ],
      "issue": 6,
      "state": "open"
    },
    {
      "id": "t-005",
      "title": "Stale-claim report tool for the evening /verify pass",
      "contract": {
        "inputs": "state/claims.json and state/tasks.json, read directly from disk with no npm dependency, plus an optional --days flag (default 3).",
        "outputs": "tools/stale-claims.js: a CLI script that prints each claim whose claimed_at is older than the threshold, alongside its task id and title, in a format /verify's report step can read or paste directly.",
        "invariants": [
          "Runs with a bare `node tools/stale-claims.js`, no npm install, consistent with the rest of tools/** staying dependency-free.",
          "Age is computed from claimed_at in UTC, compared against the current time, using the same RFC3339 parsing the claims schema documents.",
          "Exits 0 whether or not any stale claims are found; exit code reflects a script error, not the presence of stale claims."
        ],
        "must_not_break": [
          "tools/validate-state.js remains the only script state writers must pass before committing; this tool never writes state.",
          "state/schema/claims.json and state/schema/task.json stay the source of truth for field names this script reads."
        ]
      },
      "open_decisions": [
        "Whether staleness is judged purely by claimed_at age, or also checks for an open PR referencing the task (via `gh pr list`) and only flags claims with neither — more useful, but adds a network call and a `gh` dependency to a tool that has had none.",
        "Whether output is a human-readable table for pasting into /verify's report, or JSON for a future consumer to parse — human-first is simpler now but forces a rewrite if something automates on top of it later.",
        "Whether the --days threshold is a CLI flag, an environment variable, or hardcoded to match the '~3 days' the runbook and /verify already mention informally."
      ],
      "touched_paths": [
        "tools/stale-claims.js"
      ],
      "depends_on": [],
      "issue": 7,
      "state": "open"
    },
    {
      "id": "t-006",
      "title": "CI check that a PR's diff stays inside its task's touched_paths",
      "contract": {
        "inputs": "A pull request's diff (`git diff --name-only` between base and head) and the task it claims to implement, found the same way /verify finds it: a `Task: t-XXX` line in the PR body, falling back to the task/t-XXX branch name.",
        "outputs": "A new job in .github/workflows/pr.yml that looks up that task's touched_paths in state/tasks.json (read from the base ref, since the guard job already forbids editing state/ in the PR) and fails the check with the offending file names if any changed file falls outside every listed glob.",
        "invariants": [
          "Uses the same glob-intersection semantics as tools/validate-state.js — a file matches a directory glob like packages/bot/** — so a false failure never blocks a legitimately in-scope change.",
          "The task lookup logic matches .claude/commands/verify.md's own rule: prefer the Task: t-XXX body line, fall back to the task/t-XXX branch name, and treat a PR that matches neither as suspect.",
          "The job reads state/tasks.json only from the base branch's ref, never from the PR head, since a PR is forbidden from editing state/ anyway."
        ],
        "must_not_break": [
          "The existing guard job in .github/workflows/pr.yml keeps failing any PR that edits state/ or tests/, independent of this new job.",
          "PR body text is passed through env, never interpolated directly into a shell command, matching the existing decisions job's handling of attacker-controlled input."
        ]
      },
      "open_decisions": [
        "Whether the glob matcher is copied from tools/validate-state.js's self-contained function, or that function is exported and required directly from the workflow's script step — duplication versus a new implicit dependency between tools/ and .github/.",
        "Whether a PR that names no task fails this new job outright, or only warns and lets the existing 'What I decided' check be the hard gate for that case.",
        "Whether an out-of-scope file fails the whole job immediately, or the job lists every offending file across the diff before failing, to save the contributor a round trip."
      ],
      "touched_paths": [
        ".github/workflows/pr.yml"
      ],
      "depends_on": [],
      "issue": 8,
      "state": "open"
    },
    {
      "id": "t-007",
      "title": "Serverless HTTP interactions endpoint, replacing the gateway bot",
      "contract": {
        "inputs": "Discord interaction POSTs, each signed with Ed25519 and carrying a timestamp header, delivered to a public HTTPS endpoint instead of arriving over a gateway websocket.",
        "outputs": "packages/bot serves the same four commands from a request handler with no long-lived process. Signature verification, a deferred acknowledgement inside Discord's 3-second deadline, and a follow-up edit once the GitHub work finishes.",
        "invariants": [
          "Every request is Ed25519-verified against the application public key before anything else happens. An unverified or replayed request is answered 401 and does nothing.",
          "A PING (type 1) is answered with a PONG (type 1). Discord sends one to validate the endpoint and will not save it otherwise.",
          "Commands acknowledge with a deferred response immediately and edit the original message when the work completes, because reading three GitHub repos does not fit in 3 seconds.",
          "eligibility.ts, service.ts and format.ts are untouched. The hosting model is a shell concern; if a claim rule has to change to make this work, the seam was wrong.",
          "No Node-only APIs on the request path — no Buffer, no node:crypto — so the same handler runs on a worker runtime and under Node."
        ],
        "must_not_break": [
          "The claim rules and their tests. Two users still cannot claim overlapping tasks, and one person still holds one claim across all projects.",
          "packages/state stays the only code that talks to GitHub."
        ]
      },
      "open_decisions": [
        "Whether to keep the discord.js gateway entry alongside the HTTP one for local development, or delete it. Two runtimes drift; one runtime means local testing needs a tunnel.",
        "Where the follow-up work runs after the deferred acknowledgement — waitUntil on the worker platform, or a queue. waitUntil is far simpler and bounded by the platform's CPU limit, which a three-repo read may or may not fit inside.",
        "Whether to drop @octokit/rest for plain fetch in packages/state. Octokit is a large dependency for two API calls and its Node assumptions are what force compatibility flags on a worker runtime."
      ],
      "touched_paths": [
        "packages/bot/**",
        "packages/state/src/store.ts"
      ],
      "depends_on": [
        "t-002"
      ],
      "issue": 11,
      "state": "merged"
    },
    {
      "id": "t-008",
      "title": "Post a merge announcement to #merged from /verify",
      "contract": {
        "inputs": "A task and PR that a human has just approved for merge inside .claude/commands/verify.md's step 5 ('On an approved merge'): the task id, title, the PR number, and the already-configured DISCORD_CHANNEL_MERGED value plus a bot token available to the human's shell (the same DISCORD_TOKEN documented in docs/discord-setup.md and .env.example).",
        "outputs": ".claude/commands/verify.md gains a final sub-step in section 5, after the state commit and push, that posts one message naming the task id, title and merged PR link to the #merged channel via a plain authenticated HTTPS call to Discord's REST API (POST /channels/{channel_id}/messages with an Authorization: Bot <token> header) — the same endpoint packages/bot/src/discord/rest.ts already calls, but issued from the human's own shell during /verify rather than from the bot.",
        "invariants": [
          "The announcement step runs only after the state commit and push in step 5 have already succeeded, since the merge itself must never be blocked or rolled back by a Discord failure.",
          "A failed or skipped post is reported in /verify's closing summary (section 6) rather than silently swallowed, so a human notices #merged has gone quiet.",
          "The token and channel id are read from the human's existing environment (the same values used to run the bot), never hardcoded or committed into .claude/commands/verify.md.",
          "The message identifies the task id, its title, and a link to the merged PR."
        ],
        "must_not_break": [
          "The existing merge sequence in verify.md section 5 (squash-merge, update state, validate, commit, push) keeps the same order and the same 'must pass before you commit' rule for the validator.",
          "Section 6's closing report still lists what remains open and which claims are stale, in addition to the new announcement note."
        ]
      },
      "open_decisions": [
        "Whether the HTTPS call is a literal curl command written into verify.md for the human/agent running it to execute, or a small helper script under tools/ that verify.md invokes — a curl one-liner needs no new file, a script is reusable and testable.",
        "Whether a missing or invalid token/channel id fails section 5 outright (treating the announcement as part of 'an approved merge') or only degrades to a note in the closing summary, leaving the merge itself unaffected.",
        "Whether the message is posted once per merged PR immediately, or batched into a single message listing every task merged in that evening's /verify pass."
      ],
      "touched_paths": [
        ".claude/commands/verify.md"
      ],
      "depends_on": [],
      "issue": 13,
      "state": "open"
    },
    {
      "id": "t-009",
      "title": "Post the daily generator report to #start via webhook",
      "contract": {
        "inputs": "The `=== ACTION REPORT ===` block the daily routine already produces at the end of every run (per tools/routine-prompt.md), plus a Discord webhook URL for the #start channel — the channel docs/discord-setup.md already documents as 'where /task is run and the daily report lands' but which nothing currently posts to.",
        "outputs": "tools/routine-prompt.md gains a final numbered step, after the existing step 9 ('Final report'), instructing the routine to POST the report text to the #start channel using a bare curl call to a Discord incoming webhook URL (no npm dependency, matching the routine's existing constraint of a fresh sandbox with no install step) — with docs/runbook.md documenting where that webhook URL is provisioned for the human operating the routine.",
        "invariants": [
          "The webhook call uses only curl, matching every other network operation the routine already performs (git, gh) with zero additional dependency.",
          "The webhook URL is never printed in the routine's own report or committed to the repo — docs/runbook.md documents it as a value the human sets on the routine's own stored configuration, the same out-of-repo mechanism already used for tools/routine-prompt.md itself.",
          "A failed webhook POST does not undo, retry-loop, or block the git push that already happened in step 8; it is noted under this run's ACTION REQUIRED BY HUMAN instead.",
          "Discord's 2000-character message limit is respected — a report longer than that is truncated with a note, not silently rejected by the API."
        ],
        "must_not_break": [
          "The routine still prints the full, untruncated === ACTION REPORT === to its own run log regardless of whether the webhook post succeeds.",
          "Step 7 (the validator gate) and step 8 (commit and push) keep running exactly as they do today, unaffected by whether a webhook URL is configured."
        ]
      },
      "open_decisions": [
        "Whether the full report is posted verbatim (risking truncation at 2000 characters on a busy day) or condensed to the Status/Tasks/ACTION REQUIRED lines with a link back to the commit.",
        "Whether a missing webhook URL is silently skipped (the routine has run for months without this) or reported as a one-line ACTION REQUIRED BY HUMAN note so the gap gets noticed and fixed.",
        "Whether the webhook posts on every run regardless of outcome, or only when there is something worth surfacing (tasks added, or a stop condition) — an operator watching #start versus one who only wants to be told when something needs attention."
      ],
      "touched_paths": [
        "tools/routine-prompt.md",
        "docs/runbook.md"
      ],
      "depends_on": [],
      "issue": 14,
      "state": "open"
    },
    {
      "id": "t-010",
      "title": "Retry a Discord 429 rate limit once in DiscordRest",
      "contract": {
        "inputs": "Any call through packages/bot/src/discord/rest.ts (editOriginalResponse, createThread, postMessage, registerGuildCommands) whose HTTP response from Discord is 429, carrying a Retry-After header and a JSON body with a retry_after field, per Discord's documented rate-limit response.",
        "outputs": "The shared request path in rest.ts waits the indicated time and retries the same request exactly once before giving up, so a single burst of commands (e.g. several people running /task around the same moment, or the nightly integration suite) does not surface a rate-limit error to a contributor when a short wait would have succeeded.",
        "invariants": [
          "A 429 is retried at most once per call; a second consecutive 429 throws the same Error shape rest.ts already throws today, unchanged in wording for any other status code.",
          "The wait duration comes from Discord's response (the JSON body's retry_after or the Retry-After header), not a fixed or guessed backoff.",
          "Every non-429 error status continues to throw immediately with no added delay, exactly as today.",
          "The retry adds no Node-only API (no Buffer) — only what already runs unchanged on the worker runtime."
        ],
        "must_not_break": [
          "The public signatures and return types of editOriginalResponse, createThread, postMessage, and registerGuildCommands are unchanged.",
          "The follow-up work in http.ts still completes inside the waitUntil CPU budget for the normal case (no 429); the retry path only adds delay when Discord itself signals a rate limit."
        ]
      },
      "open_decisions": [
        "Whether the retry logic wraps every call site through one shared internal helper (touching #request and the two fetch calls in editOriginalResponse), or is added only to the methods most likely to be hit under contention — one consistent code path versus a smaller diff.",
        "Whether a wait longer than some cap (e.g. a couple of seconds) is honoured in full or treated as not worth retrying, given the worker's CPU-time budget under waitUntil.",
        "Whether retry_after from the JSON body or the Retry-After header wins when a response somehow carries both and they disagree."
      ],
      "touched_paths": [
        "packages/bot/src/discord/rest.ts"
      ],
      "depends_on": [],
      "issue": 15,
      "state": "open"
    },
    {
      "id": "t-011",
      "title": "Cloudflare Worker scheduled trigger for a stale-claim nag in #code",
      "contract": {
        "inputs": "The worker's existing Cloudflare bindings (env, ctx), plus state/claims.json and state/tasks.json read through the already-merged packages/state client (StateStore / TaskService.loadAll), and the isStale/claimAgeHours/STALE_CLAIM_HOURS helpers already exported from packages/bot/src/eligibility.ts. A new Cloudflare Cron Trigger fires scheduled(event, env, ctx) on the worker.",
        "outputs": "packages/bot/src/worker.ts exports a scheduled handler alongside the existing fetch handler; a new packages/bot/src/scheduled.ts holds the logic (loop every configured project, find claims where isStale() is true, post one Discord message per stale claim to #code via DiscordRest.postMessage, imported unmodified from discord/rest.ts). packages/bot/wrangler.toml gains a [triggers] block with a crons entry.",
        "invariants": [
          "No Node-only API (Buffer, node:crypto, fs) is introduced — the worker still runs with no nodejs_compat flag.",
          "eligibility.ts, service.ts, format.ts and discord/rest.ts are imported as-is, not modified — the hosting/scheduling model is a shell concern, exactly as t-007 established for the HTTP path.",
          "The scheduled handler reads with { fresh: true } (or accepts the 30s cache) but never writes to state/claims.json or state/tasks.json — nagging is read-only, it does not release or touch claims.",
          "A failure reading one project's state does not stop the other configured projects from being checked in the same run."
        ],
        "must_not_break": [
          "The existing fetch handler and every slash command behave exactly as before; this is a strictly additive export.",
          "packages/bot/wrangler.toml's existing [vars] and the no-nodejs_compat comment stay in place."
        ]
      },
      "open_decisions": [
        "Whether the nag posts once per stale claim per cron run (repeating every tick until released — persistent but noisy) or the worker needs some way to remember it already nagged this claim (quieter, but there is no persistence layer available to a worker with no KV/D1 binding configured today).",
        "Whether the message @-mentions the claimant directly or names them without a ping — a direct ping is more likely to be seen but is also the kind of thing that gets a bot muted.",
        "The cron cadence (e.g. once daily vs. every few hours) — trading how quickly a stale claim gets surfaced against extra worker invocations and Discord messages for a five-person bot."
      ],
      "touched_paths": [
        "packages/bot/src/worker.ts",
        "packages/bot/src/scheduled.ts",
        "packages/bot/wrangler.toml"
      ],
      "depends_on": [],
      "issue": 16,
      "state": "open"
    },
    {
      "id": "t-012",
      "title": "Backfill missing GitHub issues for tasks with issue: null",
      "contract": {
        "inputs": "state/tasks.json from each repo in GITHUB_TOKEN's scope (read the same way tools/check-credentials.mjs reads config — a .env file, dependency-free fetch), filtered to tasks where \"issue\": null.",
        "outputs": "tools/backfill-issues.mjs, a CLI script (node tools/backfill-issues.mjs [--repo owner/name] [--apply]) that, for each task with issue: null, calls GET /search/issues for an existing open or closed issue whose title exactly matches the task's title in that repo; if none is found it calls POST /repos/{owner}/{repo}/issues to create one from the task's title and contract text. Without --apply it only prints what it would do; with --apply it also performs a state/tasks.json read-modify-write (GET blob+sha, set issue, PUT with that sha, matching the sha-retry-once pattern packages/state's StateStore.write already uses) so the task's issue field is filled in.",
        "invariants": [
          "Dependency-free: Node 22's built-in fetch, no npm install, matching every other script in tools/.",
          "Never creates a second issue for a task that already has a non-null issue, and never touches a task's state, title, contract, open_decisions, touched_paths or depends_on — the only field it ever writes is issue.",
          "The state/tasks.json write in --apply mode uses the same PUT-with-sha, retry-once-on-409 pattern as packages/state's StateStore.write, and node tools/validate-state.js still passes after it runs.",
          "Secret values (the token) are never printed, matching tools/check-credentials.mjs."
        ],
        "must_not_break": [
          "tools/validate-state.js remains the only gate a state writer must pass before the write is considered final; this tool must run it (or fail loudly) after an --apply write, not assume success.",
          "tools/stale-claims.js (t-005) and this script stay independent — this one never reads or writes state/claims.json."
        ]
      },
      "open_decisions": [
        "Whether the search-before-create step (GET /search/issues by exact title) is worth the extra API call and the risk of a false-positive match on a coincidentally identical title, versus always creating fresh and accepting an occasional duplicate — the runbook's documented workaround today is \"add the issues by hand,\" which already risks duplicates.",
        "Whether --apply's state write is in scope for this tool at all, or whether it should only ever print suggested issue numbers for a human to paste into state/tasks.json by hand, given that state/tasks.json today is written only by the routine and by /verify.",
        "Whether a task in a non-open state (e.g. claimed or in_review) with a null issue is backfilled the same as an open one, or skipped as a stranger case that a human should look at directly."
      ],
      "touched_paths": [
        "tools/backfill-issues.mjs"
      ],
      "depends_on": [],
      "issue": 17,
      "state": "open"
    },
    {
      "id": "t-013",
      "title": "Task cycle-time report from state/tasks.json git history",
      "contract": {
        "inputs": "Git history of state/tasks.json in one or more repos (git log -p -- state/tasks.json in a local clone, or the equivalent via the GitHub commits API for a repo this process only has a token for), read to find the commits where each task's state field changed (the routine and /verify both commit with a message naming the task id, e.g. \"state: t-004 claimed\", \"tasks: add t-011, t-012, t-013\").",
        "outputs": "tools/task-stats.mjs, a dependency-free CLI script (node tools/task-stats.mjs [--repo owner/name]) that prints, per task that has reached merged or rejected, the elapsed time between its open commit and its final commit, and a small summary across all such tasks (count merged, count rejected, median and longest time-to-resolution).",
        "invariants": [
          "Dependency-free: Node 22's built-in fetch and child_process (for local git log) only, no npm install.",
          "Never writes to state/ — purely a read/report tool, like tools/stale-claims.js.",
          "A task whose full history cannot be reconstructed (e.g. the repo was shallow-cloned) is reported as \"unknown\", not silently omitted from the summary counts and not treated as a script error.",
          "Exits 0 whether or not any merged/rejected tasks are found; a non-zero exit means the script itself failed, not that the numbers are uninteresting."
        ],
        "must_not_break": [
          "tools/stale-claims.js (t-005) stays the tool for claims currently open; this one only ever looks at tasks that have already reached a terminal state.",
          "tools/validate-state.js remains untouched and is not a dependency of this script."
        ]
      },
      "open_decisions": [
        "Whether time-to-resolution is measured from the task's open commit or from its claimed commit (from the routine's perspective vs. from the point someone actually started work) — the two answer different questions about where time is actually going.",
        "Whether the tool reads git history from a local clone only (fast, but only covers whichever repo the script is run inside) or also supports reading a sibling repo's history through the GitHub API using GITHUB_TOKEN (consistent with the project serving three repos from one process, at the cost of a network dependency this tool would not otherwise need).",
        "Whether the output is a human-readable table (consistent with tools/stale-claims.js's current format) or CSV, given specs/project.md's framing of git history as \"the experiment's dataset\" that something may eventually analyze programmatically."
      ],
      "touched_paths": [
        "tools/task-stats.mjs"
      ],
      "depends_on": [],
      "issue": 18,
      "state": "open"
    }
  ]
}
